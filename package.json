{
  "name": "gls-pos",
  "version": "0.0.1",
  "private": true,
  "description": "GLS-POS - a Zobaze-style point of sale system (Expo + Hono monorepo)",
  "packageManager": "pnpm@10.29.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.5.5",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd",
      "@shopify/react-native-skia"
    ]
  }
}
